name: hyper_calendar
description: A Calendar project made by Chanadu.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '^3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_colorpicker: ^1.0.3
  provider: ^6.0.5
  firedart: ^0.9.2
  intl: ^0.18.1
  mongo_dart: ^0.9.1
  collection: ^1.17.1
  jiffy: ^6.2.1
  fixnum: ^1.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/HyperCalendarLogo.png

