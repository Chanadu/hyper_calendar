import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

import 'date_input_list_tile.dart';

class RepetitionSelector extends StatelessWidget {
  const RepetitionSelector({super.key});

  @override
  Widget build(BuildContext context) {
    //bool isAvailable = context.watch<DateTimeModel>().isOneDayEvent;

    //if (isAvailable) return const Text("Not avaliable");
    return const Text("Ok");
  }
}
